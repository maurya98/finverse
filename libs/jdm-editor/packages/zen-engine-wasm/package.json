{
  "name": "@gorules/zen-engine-wasm",
  "version": "0.22.1",
  "description": "ZEN Engine WASM",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "sideEffects": [
    "./dist/zen_engine_wasm.js",
    "./dist/snippets/*"
  ],
  "files": [
    "dist/",
    "util/",
    "index.js",
    "index.d.ts",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p dist && (wasm-pack build --out-dir dist --target web --no-pack --release 2>/dev/null && rm -f dist/.gitignore || echo '⚠️  WASM build skipped')",
    "build:debug": "wasm-pack build --out-dir dist --target web --no-pack --dev && rm dist/.gitignore",
    "prepublishOnly": "pnpm run build"
  },
  "devDependencies": {
    "wasm-pack": "^0.13.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
