info:
  name: Get Users By Role (ADMIN)
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3002/api/users/role/ADMIN
  headers:
    - name: Authorization
      value: Bearer your_auth_token_here
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
