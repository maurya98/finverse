# Production environment for LMS Backend
# Copy to .env and fill in values.

NODE_ENV=production
PORT=3002

# Postgres (installed on server)
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/lms

# Optional: KYC integration
# KYC_BASE_URL=https://svcdemo.digitap.work
# KYC_AUTH_BASIC=
# CLIENT_REF_NUM=
