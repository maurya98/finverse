# Production environment for Site Platform Backend
# Copy to .env and fill in values.

NODE_ENV=production
PORT=5001

# Postgres (installed on server)
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/site_platform

# Redis (installed on server; use localhost when using network_mode: host)
REDIS_HOST=localhost
REDIS_PORT=6379
